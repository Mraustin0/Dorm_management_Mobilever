<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Horganized">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".user.HomeUserActivity" />
        <activity android:name=".user.DetailBillActivity" />
        <activity android:name=".user.DormInfoActivity" />
        <activity android:name=".user.UserProfileActivity" />
        <activity android:name=".user.UserProfileEditActivity" />
        <activity android:name=".user.ChatActivity" />
        <activity android:name=".user.PayBillActivity" />
        <activity android:name=".user.PayBillConfirmActivity" />
        <activity android:name=".user.NotificationsActivity" />
        <activity android:name=".user.RepairActivity" />
        <activity android:name=".user.AnnouncementActivity" />
        <activity android:name=".user.ContractListActivity" />
        <activity android:name=".user.ContractDetailActivity" />
        <activity android:name=".MainActivity" />

    </application>

</manifest>